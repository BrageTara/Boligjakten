# 2026-04-27

## Done today

- Set up `_run_scraper.bat` wrapper for scheduled execution
- Discovered and fixed UTF-8 encoding bug (added `PYTHONIOENCODING=utf-8`)
- Created Windows scheduled task pointing at the new `.bat`
- Created this Obsidian vault

## Open issues

- Playwright `Browser.close: Connection closed while reading from the driver` crash at end of scrape — not blocking, but worth investigating
- `run_log` always sets `status='success'` in the `finally` block in `finn_tracker_db.py:678`, even when scraper crashed mid-run — bug, fix later

## Notes

-
